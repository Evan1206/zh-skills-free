# 安裝｜cs-reply 免費試用

## 方式 A：直接用資料夾

1. 複製本 repo 的 `free/cs-reply-trial/` 整個資料夾。
2. 貼到 Cursor／Claude／相容 AI 工具的 **skills** 目錄（依你的工具說明放置）。
3. 重新載入 skills／重啟對話視窗。
4. 觸發方式見該目錄 `SKILL.md` frontmatter 與說明。

## 方式 B：下載 ZIP

1. 下載 [`cs-reply-free-trial.zip`](./cs-reply-free-trial.zip)（或 `releases/` 內同檔）。
2. 解壓得到 `cs-reply-trial/`。
3. 將 `cs-reply-trial/` 複製到 skills 目錄並重新載入。

## 限制

請閱讀 `free/cs-reply-trial/LIMITS.md`。需要完整版請至 Portaly（見根 README 的 `PLACEHOLDER_PORTALY`）。
